{
    "name": "viza-backend-root",
    "version": "1.0.0",
    "description": "Root package to manage backend scripts",
    "scripts": {
        "start": "npm start --prefix server",
        "dev": "npm run dev --prefix server",
        "install-all": "npm install --prefix server"
    },
    "dependencies": {
        "dotenv": "^17.2.4",
        "jspdf": "^4.1.0",
        "mongoose": "^9.2.0"
    }
}
